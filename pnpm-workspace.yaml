allowBuilds:
  esbuild: false
  sharp: true
  unrs-resolver: true
  # Descarga su binario, que es el que sube los source maps al construir. Sin él la traza de
  # producción llega minificada.
  "@sentry/cli": true
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
overrides:
  postcss: '>=8.5.18'
  sharp: '>=0.35.0'
  esbuild: '>=0.25.0'
